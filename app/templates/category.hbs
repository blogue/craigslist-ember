<h1>{{model.name}}</h1>
<ul>
  {{#each model.listings as |listing|}}
  <li>{{#link-to 'listing' listing.id}}{{listing.title}} {{listing.date}}{{/link-to}}</li>
  {{/each}}

</ul>

{{new-listing save="save" category=model}}
